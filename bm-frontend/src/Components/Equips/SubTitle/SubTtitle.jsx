import React from 'react';
import './equip-subtitle.css';
import { Divider } from '@mui/material';

const SubTtitle = ({ title }) => {
  /* Router */
  /* State */
  /* Functions */
  /* Hooks */
  /* Render */
  return (
    <div className="equip-subtitle-container">
      <div className="subtitle">{title}</div>
      <Divider />
    </div>
  );
};

export default SubTtitle;
